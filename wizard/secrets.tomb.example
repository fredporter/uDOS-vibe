# wizard/secrets.tomb.example
# This is an empty stub. The real secrets.tomb is generated by SETUP
# and stored locally only — it is never committed to git.
#
# To regenerate a fresh tomb:
#   python wizard/tools/reset_secrets_tomb.py
#
# The tomb is an encrypted JSON store keyed by WIZARD_KEY in .env.
# Format (when decrypted): {"key_name": "value", ...}
