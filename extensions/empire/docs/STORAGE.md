# Empire Storage (SQLite)

Default database: `empire/data/empire.db`

Tables:
- `records` — HubSpot-aligned contact fields + raw JSON snapshot
- `sources` — ingestion sources
- `events` — activity log / engagement timeline
