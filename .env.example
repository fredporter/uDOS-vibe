# uDOS-vibe Environment — Essential Variables Only
# See: core/config/config.toml for app settings (logging, UI, tuning, etc.)
# See: wizard/secrets.tomb for API keys and credentials (encrypted)
#
# ✅ SAFE TO COMMIT — No secrets here
# Setup: cp .env.example .env, then run: python uDOS.py

# Core paths (required at startup)
UDOS_ROOT=/Users/yourname/Code/uDOS-vibe
VAULT_ROOT=${UDOS_ROOT}/memory/vault
VAULT_MD_ROOT=${UDOS_ROOT}/memory/vault

# Identity (set by SETUP; 'Ghost' enables testing mode)
USER_NAME=Ghost

# Wizard server (endpoint for dashboard/API)
WIZARD_BASE_URL=http://localhost:8765

# Development flags (for local testing/CI)
UDOS_DEV_MODE=0
UDOS_TEST_MODE=0
UDOS_AUTOMATION=0
UDOS_DEFAULT_USER=
