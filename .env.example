# ============================================================
# uDOS-vibe — Environment Configuration Template
#
# ✅ SAFE TO COMMIT — This file contains no secrets
# Copy to .env and fill in your values before first run.
# Run: cp .env.example .env
# Then: python uDOS.py (SETUP story will complete the rest)
#
# 🔐 SECURITY: Never commit real API keys to this file!
#    See "SECURITY NOTES" section at the bottom.
# ============================================================

# ── REQUIRED: Core paths ────────────────────────────────────
# Absolute path to this repo root (must contain uDOS.py)
# Auto-detected from file path if not set, but explicit is safer.
UDOS_ROOT=/Users/yourname/Code/uDOS-vibe

# Vault root — framework wireframe lives at vault/, runtime user
# vault is seeded into memory/vault/ by SETUP/REPAIR automatically.
VAULT_ROOT=${UDOS_ROOT}/memory/vault

# Markdown vault alias (used by some services)
VAULT_MD_ROOT=${UDOS_ROOT}/memory/vault

# ── REQUIRED: Identity ───────────────────────────────────────
# Set by SETUP story. Username 'Ghost' forces Ghost Mode (demo/limited).
USER_NAME=Ghost

# ── REQUIRED: Locale ─────────────────────────────────────────
UDOS_TIMEZONE=UTC
UDOS_LOCATION=grid-code
UDOS_LOCATION_NAME=Greenwich
UDOS_GRID_ID=

# ── AI / Mistral (vibe-cli) ──────────────────────────────────
# 🔐 DO NOT PUT YOUR ACTUAL API KEY HERE!
# Mistral keys belong in wizard/secrets.tomb (encrypted store)
# Leave this empty, or set via Wizard dashboard:
#   1. Run: WIZARD start
#   2. Visit: http://localhost:8765/dashboard
#   3. Settings > Integrations > Add Secret > Mistral
# Get credentials: https://console.mistral.ai
MISTRAL_API_KEY=

# ── Wizard server ────────────────────────────────────────────
# 🔐 DO NOT PUT ACTUAL TOKENS HERE!
# These are auto-generated by SETUP story:
#   - WIZARD_KEY: Encryption key for secrets.tomb (leave empty)
#   - WIZARD_ADMIN_TOKEN: Access token (leave empty, auto-generated)
# Do NOT manually set unless resetting. See: wizard/tools/reset_secrets_tomb.py
WIZARD_ADMIN_TOKEN=
WIZARD_BASE_URL=http://localhost:8765

# Encryption key for identity store (auto-generated by SETUP)
# Leave empty unless manually resetting secrets.tomb
WIZARD_KEY=

# ── Optional: AI providers (Wizard) ─────────────────────────
# 🔐 DO NOT PUT ACTUAL KEYS HERE!
# All API keys belong in secrets.tomb via Wizard dashboard
OLLAMA_HOST=http://localhost:11434
EMPIRE_API_TOKEN=

# ── OS / Environment ─────────────────────────────────────────
# One of: mac, ubuntu, alpine, windows
OS_TYPE=mac

# ── Logging ──────────────────────────────────────────────────
UDOS_LOG_LEVEL=INFO
UDOS_LOG_DEST=file
UDOS_LOG_FORMAT=json
UDOS_LOG_CATEGORIES=
UDOS_LOG_PAYLOADS=false
UDOS_LOG_REDACT=true
UDOS_LOG_RING=false
UDOS_LOG_SAMPLING=false
UDOS_LOG_ROOT=${UDOS_ROOT}/memory/logs

# ── TUI behaviour ────────────────────────────────────────────
UDOS_QUIET=0
UDOS_NO_ANSI=0
UDOS_EMOJI_TUI_RENDER=1
UDOS_TUI_CLEAN_STARTUP=1
UDOS_TUI_STARTUP_EXTRAS=0
UDOS_TUI_FULL_METERS=0
UDOS_TUI_INVERT_HEADERS=0
UDOS_TUI_MAP_LEVEL=1
UDOS_TUI_FORCE_STATUS=0
UDOS_LAUNCHER_BANNER=1
UDOS_STORY_FORM_TUI=1
UDOS_KEYMAP_PROFILE=auto

# ── Dev / automation flags ───────────────────────────────────
# Set UDOS_AUTOMATION=1 to suppress interactive prompts in CI
UDOS_AUTOMATION=0
UDOS_DEV_MODE=0
UDOS_TEST_MODE=0
UDOS_SESSION_ID=
UDOS_DEFAULT_USER=
UDOS_HOME_ROOT_ENFORCE=0
UDOS_SKIP_DEP_CHECK=0
UDOS_DISABLE_DEP_WARNING_MONITOR=0
UDOS_OLLAMA_AUTOSTART=0

# ── Viewport (auto-detected at startup, override if needed) ──
UDOS_VIEWPORT_COLS=
UDOS_VIEWPORT_ROWS=

# ── Compost / cleanup limits ─────────────────────────────────
UDOS_COMPOST_MAX_BYTES=
UDOS_COMPOST_MAX_MB=
UDOS_COMPOST_RESERVE_MB=

# ── Vibe-cli tuning ──────────────────────────────────────────
VIBE_STREAM_DELAY_MS=0

# ── Terminal ─────────────────────────────────────────────────
# Usually set by the shell — only override if terminal detection breaks
# TERM and NO_COLOR are intentionally unset by default.
# Setting empty values can break interactive CLI rendering/input.
# TERM=
# NO_COLOR=
UCODE_VERSION=1.0.1

# ── Optional provider overrides (disabled by default) ───────
# VIBE_PRIMARY_PROVIDER=cloud
# VIBE_SECONDARY_PROVIDER=cloud
# ──────────────────────────────────────────────────────────────
# 🔐 SECURITY NOTES
# ──────────────────────────────────────────────────────────────
#
# ✅ What goes in .env:
#    • Non-sensitive config (paths, timezone, locale, user name)
#    • Feature flags (logging level, TUI behavior, automation)
#    • WIZARD_KEY (only if you're resetting — normally auto-generated)
#
# ❌ What NEVER goes in .env:
#    • API keys (Mistral, OpenAI, Anthropic, GitHub, AWS, etc.)
#    • OAuth tokens (Gmail, Google Drive, etc.)
#    • Cloud credentials (AWS keys, GCP service accounts, Azure, etc.)
#    • WIZARD_ADMIN_TOKEN (auto-generated, stored in secrets.tomb)
#    • Webhook secrets, encryption keys, personal access tokens
#
# 🔐 Where to put API keys and secrets:
#    1. Leave them EMPTY in .env
#    2. Start Wizard: WIZARD start
#    3. Visit dashboard: http://localhost:8765/dashboard
#    4. Click Settings > Integrations > Add Secret
#    5. Wizard encrypts it and stores in wizard/secrets.tomb (Fernet)
#
# 🛡️  If you see secrets in git history:
#    An incident may have occurred. See: docs/devlog/2026-02-24-security-audit.md
#    • All exposed credentials must be rotated
#    • Run: git-filter-repo to clean history (contact team lead)
#
# 🛠️  Troubleshooting secrets:
#    • Check status: uv run wizard/tools/check_secrets_tomb.py
#    • Reset secrets: uv run wizard/tools/reset_secrets_tomb.py
#    • Rotate admin token: Wizard dashboard > Settings > Secret Store
#
# 📖 Full security documentation:
#    • docs/devlog/2026-02-24-security-audit.md
#    • docs/specs/ENV-STRUCTURE-V1.1.0.md
#
# ────────────────────────────────────────────────────────────────
